namespace Compze.Build.FlexRef.CLI;

static class CliConstants
{
    public const string CommandName = "flexref";

    public static class Commands
    {
        public const string Init = "init";
        public const string Sync = "sync";
    }
}
